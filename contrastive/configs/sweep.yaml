program: train.py
project: flanker-test
method: bayes
metric:
  goal: minimize
  name: gs_criterion
parameters:
  lr:
    # distribution: log_uniform_values
    min: 0.00035
    max: 0.001
  drop_rate:
    min: 0.15
    max: 0.35
  gamma:
    min: 0.7
    max: 1.0
  # sigma_noise:
  #   min: 0.
  #   max: 0.5
  # max_angle:
  #   min: 0
  #   max: 6

command:
  - ${env}
  - python3
  - ${program}
  - ${args_no_hyphens}